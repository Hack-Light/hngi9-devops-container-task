version: "3.1"
services:
  frontend: 
    image: hacklight/hngi9-fe:1.0 # change this your docker registry uri
    restart: always
    ports:
      - 3000:3000
    
  api:
    image: hacklight/hngi9-api:1.0 # change this your docker registry uri
    restart: always
    ports:
      - 8000:8000
